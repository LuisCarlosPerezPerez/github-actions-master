# CI CD intro

Simple repo for configuring a simple github action

Runs an action on PR that tests the code

For running the tests locally, first install de dependencies:

```sh
npm i
```

And then, run the test by:

```sh
npm test
```
